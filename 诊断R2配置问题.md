# Functions 已部署成功！现在需要解决配置问题

## ✅ 好消息

通过 curl 测试，Functions **已经正常部署**：
- `/api/health` ✅ 工作正常
- `/api/r2/list-documents` ✅ 正常响应（返回 R2 错误，这是预期的）
- `/api/r2/download-metadata-bundle` ✅ 端点存在

## 🔍 当前问题

### 1. 浏览器显示 404
**原因**：浏览器缓存了旧的部署

**解决方法**：
1. 按 `Ctrl + Shift + R` 强制刷新页面
2. 或清除浏览器缓存
3. 或使用无痕模式打开 https://spec-companion.pages.dev

### 2. R2 返回 403 Forbidden
**原因**：R2 配置不正确或凭证有误

**可能的问题**：
- Account ID 不正确
- Access Key ID 或 Secret Access Key 有误
- API Token 权限不足
- Bucket 名称错误

## 📝 诊断步骤

### 第一步：清除浏览器缓存

```
1. 按 Ctrl + Shift + Delete 打开清除浏览器数据
2. 选择"缓存的图像和文件"
3. 时间范围选择"全部时间"
4. 点击"清除数据"
5. 刷新页面
```

### 第二步：验证 R2 配置

在 R2 测试页面（`?test=r2`），你说测试是好的。那么：

1. 打开浏览器开发者工具（F12）
2. 切换到 Network 标签
3. 在 R2 测试页面点击测试按钮
4. 查看请求详情：
   - 检查 Request Payload（请求数据）
   - 检查 Response（响应）

### 第三步：检查主应用的 R2 配置是否保存

R2 配置保存在 IndexedDB 中。检查步骤：

1. 打开浏览器开发者工具（F12）
2. 切换到 Application 标签
3. 左侧找到 IndexedDB > SpecCompanionDB > settings
4. 查看是否有 "r2SyncConfig" 记录
5. 检查配置内容是否正确

## 🎯 快速验证

打开浏览器控制台，运行以下代码查看保存的 R2 配置：

```javascript
// 打开 IndexedDB 查看配置
const request = indexedDB.open('SpecCompanionDB', 2);
request.onsuccess = (event) => {
  const db = event.target.result;
  const tx = db.transaction('settings', 'readonly');
  const store = tx.objectStore('settings');
  const getRequest = store.get('r2SyncConfig');
  getRequest.onsuccess = () => {
    console.log('R2 配置:', getRequest.result);
  };
};
```

## 🔧 如果配置确实正确但仍然 403

可能的原因：

### 1. R2 Bucket 权限问题
检查 R2 API Token 是否有正确的权限：
- Object Read ✅
- Object Write ✅
- 绑定到正确的 Bucket ✅

### 2. Account ID 格式问题
Account ID 应该是 32 位十六进制字符串，例如：
```
abcdef1234567890abcdef1234567890
```

### 3. 重新生成 API Token
有时候 API Token 可能失效或权限不足，建议：
1. 在 Cloudflare Dashboard 重新生成一个新的 R2 API Token
2. 确保权限设置为 "Object Read & Write"
3. 确保绑定到正确的 Bucket
4. 重新在应用中配置

## 🧪 本地调试

如果你想在本地环境调试，运行：

```bash
cd D:\temp\spec_pdf\spec-companion-main
npm run dev
```

然后访问 `http://localhost:3000`，看看本地是否能正常工作。

## 需要我帮你做什么？

请告诉我：
1. 强制刷新后（Ctrl + Shift + R）是否还有 404 错误？
2. R2 测试页面显示什么结果？（截图）
3. IndexedDB 中保存的 R2 配置是什么？（运行上面的 JS 代码查看）
